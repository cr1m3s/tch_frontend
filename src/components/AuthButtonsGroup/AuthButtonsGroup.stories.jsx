import { AuthButtonsGroup } from './AuthButtonsGroup';

export default {
    title: 'components/AuthButtonsGroup',
    component: AuthButtonsGroup,
};

function Template(args) {
    return <AuthButtonsGroup {...args} />;
}

export const Default = Template.bind({});
Default.args = {

};
